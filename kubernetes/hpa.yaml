apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: student-api, namespace: student-api}
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: student-api}
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource: {name: cpu, target: {type: Utilization, averageUtilization: 70}}

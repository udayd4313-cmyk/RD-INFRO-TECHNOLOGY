apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: student-api
resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
# Apply a real Secret separately, preferably through External Secrets or Sealed Secrets.

# Contributing

1. Create a feature branch.
2. Install dependencies with `pip install -r requirements.txt`.
3. Run `pytest -q` and `flake8 app tests main.py`.
4. Open a pull request using the template.

Keep commits focused, never commit credentials, and update documentation for operational changes.
